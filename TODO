-*- mode: outline -*-

* create toplevel exectuable

* srp --init
actually, i shouldn't need this until later on...

* srp --create SRP_DIR

This should create a source package

* srp --query foo.srp

Test that we can query fields from source packages

* srp --build foo.srp

This will build foo.brp.

* srp --query boo.brp

Test querying fields from brp

* srp --install boo.brp

* srp --list
* srp --query
* srp --action
* srp --uninstall
* srp --install (upgrade, no_upgrade, etc)

