-*- mode: outline -*-

* add --root flag (instead of DESTDIR or SRP_ROOT_PREFIX)

* finish implementing planned feature modules

* make sure we can pass options into --create, --build, --install

* get the --action stuff plumbed up.

* query (from srp, brp, and installed manifest)

* upgrade logic

* uninstall
